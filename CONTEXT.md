# File that explains the structure and aim of this project folder as a context for code generation.

# We want to develop a document extraction engine that can extract relevant information from invoices and classify the invoice into categories

# the architecture should allow for the NLP models to be swapped out easily while the pipelines stay in place. This also allows to quickly add additional features that rely on other models, such as multimodal models.